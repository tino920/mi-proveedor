# 📱 PODFILE ULTRA-AGRESIVO - MiProveedor
# FIX DEFINITIVO: Limpia TANTO proyectos Pods COMO proyecto Runner principal
# Solución para baseConfigurationReference hardcoded en project.pbxproj

platform :ios, '13.0'

# Configuraciones CI/CD
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

project 'Runner', {
  'Debug' => :debug,
  'Profile' => :release,
  'Release' => :release,
}

def flutter_root
  generated_xcode_build_settings_path = File.expand_path(File.join('..', 'Flutter', 'Generated.xcconfig'), __FILE__)
  unless File.exist?(generated_xcode_build_settings_path)
    raise "#{generated_xcode_build_settings_path} must exist. If you're running pod install manually, make sure flutter pub get is executed first"
  end

  File.foreach(generated_xcode_build_settings_path) do |line|
    matches = line.match(/FLUTTER_ROOT\=(.*)/)
    return matches[1].strip if matches
  end
  raise "FLUTTER_ROOT not found in #{generated_xcode_build_settings_path}. Try deleting Generated.xcconfig, then run flutter pub get"
end

require File.expand_path(File.join('packages', 'flutter_tools', 'bin', 'podhelper'), flutter_root)

flutter_ios_podfile_setup

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  # 🔥 FLUTTER MANEJA TODAS LAS DEPENDENCIAS
  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
  
  target 'RunnerTests' do
    inherit! :search_paths
  end
end

# 📱 POST-INSTALL ULTRA-AGRESIVO
post_install do |installer|
  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target)
    
    # 🎯 CONFIGURAR DEPLOYMENT TARGET
    target.build_configurations.each do |config|
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.0'
      config.build_settings['ENABLE_BITCODE'] = 'NO'
    end
  end
  
  puts "\n🔧 APLICANDO FIX ULTRA-AGRESIVO..."
  
  # 🔥 FIX 1: LIMPIAR PROYECTOS PODS (como antes)
  installer.generated_projects.each do |project|
    project.targets.each do |target|
      target.build_configurations.each do |config|
        config.base_configuration_reference = nil
      end
    end
    puts "✅ Pods project base configurations limpiadas"
  end
  
  # 🔥 FIX 2: LIMPIAR PROYECTO PRINCIPAL RUNNER (NUEVO!)
  installer.aggregate_targets.each do |aggregate_target|
    aggregate_target.user_project.targets.each do |target|
      target.build_configurations.each do |config|
        # FORZAR que CocoaPods pueda aplicar su configuración
        config.base_configuration_reference = nil
        puts "🎯 Limpiando #{target.name} - #{config.name} baseConfigurationReference"
      end
    end
  end
  
  # 🔥 FIX 3: APLICAR CONFIGURACIONES COCOAPODS MANUALMENTE
  installer.aggregate_targets.each do |aggregate_target|
    aggregate_target.user_project.targets.each do |target|
      if target.name == 'Runner'
        target.build_configurations.each do |config|
          # Forzar configuraciones CocoaPods manualmente
          case config.name
          when 'Debug'
            xcconfig_path = installer.sandbox.target_support_files_dir + "Pods-#{target.name}" + "Pods-#{target.name}.debug.xcconfig"
            config.base_configuration_reference = installer.aggregate_targets[0].user_project.new_file(xcconfig_path) if File.exist?(xcconfig_path)
            puts "🔥 Aplicando Debug CocoaPods config a Runner"
          when 'Release'
            xcconfig_path = installer.sandbox.target_support_files_dir + "Pods-#{target.name}" + "Pods-#{target.name}.release.xcconfig"
            config.base_configuration_reference = installer.aggregate_targets[0].user_project.new_file(xcconfig_path) if File.exist?(xcconfig_path)
            puts "🔥 Aplicando Release CocoaPods config a Runner"
          when 'Profile'
            xcconfig_path = installer.sandbox.target_support_files_dir + "Pods-#{target.name}" + "Pods-#{target.name}.release.xcconfig"
            config.base_configuration_reference = installer.aggregate_targets[0].user_project.new_file(xcconfig_path) if File.exist?(xcconfig_path)
            puts "🔥 Aplicando Profile CocoaPods config a Runner"
          end
        end
      end
    end
  end
  
  # ✅ MENSAJE DE ÉXITO
  puts "\n🎉 ¡FIX ULTRA-AGRESIVO APLICADO!"
  puts "🔥 Proyecto Runner: baseConfigurationReference limpiado y reconfigurado"
  puts "🔥 Proyectos Pods: baseConfigurationReference limpiado"  
  puts "🎯 CocoaPods: Ahora puede aplicar su configuración sin conflictos"
  puts "✅ BoringSSL-GRPC y frameworks: Deberían encontrarse correctamente\n"
end
